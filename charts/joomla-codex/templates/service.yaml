apiVersion: v1
kind: Service
metadata:
  name: {{ include "joomla-codex.fullname" . }}
  labels:
    {{- include "joomla-codex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "joomla-codex.selectorLabels" . | nindent 4 }}
