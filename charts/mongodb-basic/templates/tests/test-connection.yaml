apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-dok.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-dok.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-dok.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
