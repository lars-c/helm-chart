apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mongo-restore-creds
type: Opaque
stringData:
  MONGO_USER: {{ .Values.auth.rootUsername | quote }}
  MONGO_PASS: {{ .Values.auth.rootPassword | quote }}