apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-restore.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-restore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-restore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
